{
  "name": "proveo-monorepo",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "devDependencies": {
    "wrangler": "^4.11.1"
  },
  "scripts": {
    "wrangler": "wrangler",
    "sync:cli": "./scripts/sync-cli-assets.sh",
    "deploy:cli": "pnpm run sync:cli && wrangler deploy --cwd apps/cli",
    "dev:cli": "pnpm run sync:cli && wrangler dev --cwd apps/cli"
  }
}
